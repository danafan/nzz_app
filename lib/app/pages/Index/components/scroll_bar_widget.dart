import 'package:flutter/material.dart';

class ScrollBarWidget extends StatelessWidget {
  final List scrollList;
  ScrollBarWidget(this.scrollList);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}